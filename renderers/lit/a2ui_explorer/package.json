{
  "name": "@a2ui/lit-explorer",
  "private": true,
  "version": "0.9.0",
  "description": "A2UI Lit Explorer",
  "type": "module",
  "scripts": {
    "dev": "npm run build:deps && vite",
    "build:deps": "for dir in '../../web_core' '../../lit'; do npm install --prefix $dir && npm run build --prefix $dir; done && npm install",
    "build": "npm run build:deps && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@a2ui/lit": "file:..",
    "@a2ui/web_core": "file:../../web_core",
    "@a2ui/markdown-it": "file:../../markdown/markdown-it",
    "@lit-labs/signals": "^0.1.3",
    "@lit/context": "^1.1.4",
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.1.11"
  }
}
